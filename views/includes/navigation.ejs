<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Recetas de Repostería</title>
    <link rel="stylesheet" href="/style/navigation.css">
    <link rel="stylesheet" href="/style/main.css">
    <link rel="stylesheet" href="/style/auth.css">
    

 

    <style>
        
    </style>
</head>


    <header>
        <h1>Recetas</h1>
    </header>

    <nav>
        <% if (!isAuthenticated) { %>
            <div class="auth-message not-registered">
                NO REGISTRADO
            </div>
        <% } else { %>
            <div class="auth-message registered">
                Hola, <%= usuario.name.charAt(0).toUpperCase() + usuario.name.slice(1) %>
            </div>
        <% } %>

<<<<<<< HEAD
    <ul class="main-header__item-list">
        
            <li class="main-header__item">
                <a href="/login">Login</a>
            </li>
            <li class="main-header__item">
                <a href="/signup">Signup</a>
            </li>
   
            <li class="main-header__item">
                <form action="/logout" method="post">
                    <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                    <button type="submit">Logout</button>
                </form>
            </li>
 
    </ul>



</nav>
=======
        <ul>
            <li><a href="/">Inicio</a></li>
            <% if (isAuthenticated) { %>
                <li><a href="/admin/add-recipe">Añadir receta</a></li>
            <% } %>
            <li><a href="/bookmark">Recetas archivadas</a></li>
            <li><a href="#">Contacto</a></li>
            <% if (!isAuthenticated) { %>
                <li class="main-header__item"><a href="/login">Login</a></li>
                <li class="main-header__item"><a href="/signup">Signup</a></li>
            <% } else { %>
                <li class="main-header__item">
                    <form action="/logout" method="post">
                        <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                        <button type="submit">Logout</button>
                    </form>
                </li>
            <% } %>
        </ul>
    </nav>
>>>>>>> 2448dbd3db4f8145a467fb7099b75ad7a34b39b6
